# Licznik TikTok

Licznik obserwujących @pieseu.official vs @psiapsi. GitHub Actions co ~10 min uruchamia `scripts/fetch.py`, dane lądują w `data/`, strona (`index.html`) na GitHub Pages odświeża się co minutę.

Dodanie konta: dopisz nazwę do `ACCOUNTS` w `scripts/fetch.py` i kartę w `index.html`.
